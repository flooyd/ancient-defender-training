# ── Rust build output ─────────────────────────────────────────────────────────
/target/

# ── WASM build artifacts (generated by build_wasm.ps1 / build_wasm.sh) ────────
/web/client.wasm
/web/mq_js_bundle.js   # patched at build time from local cargo registry
/web/quad-net.js       # downloaded at build time

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
*.pem
*.key
*.crt
*.p12

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
